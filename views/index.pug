extends layout

block content
  h1 Video to GIF Converter
  form(action='/upload', method='post', enctype='multipart/form-data')
    input(type='file', name='video', accept='.mp4')
    button(type='submit') Convert to GIF
